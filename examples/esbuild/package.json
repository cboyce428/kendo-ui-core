{
  "name": "esbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild index.ts --bundle --outfile=index.js --servedir=. --watch",
    "serve": "lite-server .",
    "build": "esbuild index.ts --bundle --outfile=index.js",
    "build:cjs": "esbuild index_cjs.js --bundle --outfile=index.js",
    "start": "npm run build && npm run serve",
    "link-npm-pro": "npm link ../../dist/npm-pro"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@progress/kendo-ui": "2023.2.629-internal",
    "esbuild": "^0.18.6",
    "jquery": "3.7.0",
    "lite-server": "^2.6.1",
    "typescript": "^5.1.3"
  }
}
